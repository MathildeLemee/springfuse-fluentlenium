<?xml version="1.0" encoding="UTF-8"?>
<!--
 ! (c) Copyright 2005-2011 JAXIO, www.jaxio.com
 ! Source code generated by Celerio, a Jaxio product
 ! Want to use Celerio within your company? email us at info@jaxio.com
 ! Follow us on twitter: @springfuse
 ! Template pack-jsf2-primefaces:src/main/webapp/WEB-INF/flows/model-flow.e.vm.xml
-->
<flow xmlns="http://www.springframework.org/schema/webflow" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/webflow http://www.springframework.org/schema/webflow/spring-webflow-2.0.xsd">
	<!-- Extended Persistence Context -->
	<persistence-context />
	<!-- Input: -->
	<!-- * create: 'true' the first view shows the view to create a new contactInfo -->
	<!--'false' to edit an existing contactInfo -->
	<input name="create" type="boolean" value="flowScope.create"/>
	<on-start>
		<set name="conversationScope.endParam" value="null" type="java.lang.String" />
	</on-start>
	<!-- Go to the edit state if we want to create a new contactInfo -->
	<decision-state id="startChoice">
		<if test="create" then="create" else="open" />
	</decision-state>
	<!-- OPEN EXISTING ContactInfo -->
	<subflow-state id="open" subflow="contactInfo/select">
		<!-- The user has selected an existing contactInfo -->
		<transition on="contactInfoSelected" to="checkIfAlreadyOpenFlow">
			<set name="flowScope.contactInfo" value="currentEvent.attributes.contactInfoSelected" />
		</transition>
		<!-- The user finally prefers to create a new contactInfo -->
		<transition on="new" to="endNew"/>
		<!-- The user wants to quit all the active flows for the entity family, we end the current flow -->
		<transition on="quit" to="end">
			<set name="conversationScope.endParam" value="'_cascadeQuit'"></set>
		</transition>
	</subflow-state>
    
	<decision-state id="checkIfAlreadyOpenFlow">
		<if test="flowsMenuHandler.isFlowAlreadyOpen(flowRequestContext,flowScope.contactInfo)" then="endSwitchToAlreadyOpenFlow" else="edit" />
	</decision-state>
	
    <!-- Edit subflow to edit any contactInfo -->
	<subflow-state id="edit" subflow="contactInfo/edit">
		<input name="contactInfo" value="contactInfo" />
		<transition on="endEdit" to="end" />
		<transition on="cancelEdit" to="end" />
		<transition on="deleteAndClose" to="end"/>
	</subflow-state>

    <!-- Create subflow to create a new contactInfo -->
    <subflow-state id="create" subflow="contactInfo/edit">
        <input name="contactInfo" value="contactInfoService.newWithDefaults" />
        <transition on="endEdit" to="end" />
        <transition on="cancelEdit" to="end" />
        <transition on="deleteAndClose" to="end"/>
    </subflow-state>


	<end-state id="endNew" view="externalRedirect:servletRelative:/contactInfo?create=true" />

	<end-state id="end" view="externalRedirect:contextRelative:#{flowsMenuHandler.endStateRedirect(flowRequestContext,endParam)}" />

	<end-state id="endSwitchToAlreadyOpenFlow" view="externalRedirect:contextRelative:#{flowsMenuHandler.endStateRedirectToAlreadyOpenFlow(flowRequestContext,flowScope.contactInfo)}" />

	<end-state id="endError" view="externalRedirect:servletRelative:#{flowsMenuHandler.getErrorEndStateRedirect()}" />

	<global-transitions>
		<transition on="error" to="endError" />
		<transition on-exception="java.lang.RuntimeException" to="endError" />
	</global-transitions>
</flow>